# Migrations déjà fusionnées

Ces fichiers ont été intégrés directement dans `database_complet.sql` à la racine
du projet. Il n'y a plus besoin de les exécuter séparément : un import unique
de `database_complet.sql` suffit pour avoir un schéma complet et à jour.

Ils sont conservés ici uniquement pour l'historique / la traçabilité.
